using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Script used create the different Attributes for each Asteroid. | Brown Asteroids fall at normal speeds | Flaming Asteroids fall faster than Brown Asteroids. | Black Asteroids fall the slowest but
//push the player further down than the other Asteroids.
public class Asteroids : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
